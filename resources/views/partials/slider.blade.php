
<div id="carouselExampleSlidesOnly" class="carousel slide" data-bs-ride="carousel">
    <div class="carousel-inner">
        <div class="carousel-item active">
            <img src="{{ url($MainImage1->image) }}" class="d-block w-100">
        </div>
        <div class="carousel-item">
            <img src="{{ url($MainImage2->image) }}" class="d-block w-100">
        </div>
        <div class="carousel-item">
            <img src="{{ url($MainImage3->image) }}" class="d-block w-100">
        </div>
    </div>
</div>
